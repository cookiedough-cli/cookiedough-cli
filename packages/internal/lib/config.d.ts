import { CrumbOptions } from './types';
export declare function useArgParser(): {
    url: {
        parent_config: string;
    };
};
export declare function useLocalConfig(base: string): CrumbOptions;
